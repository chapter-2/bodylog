# App Auth — single password to protect your BodyLog instance
APP_PASSWORD=ESJeruk3000

# Database — path to the folder where bodylog.db will be stored
# Defaults to ./data/bodylog.db if not set
# In Docker: set this to /data (the mounted volume path)
DB_PATH=/data

