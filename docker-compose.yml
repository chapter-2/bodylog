services:
  bodylog:
    build: .
    container_name: bodylog
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - bodylog_data:/data
    environment:
      NODE_ENV: production
      DB_PATH: /data

volumes:
  bodylog_data:
    driver: local
